[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "--cov=src --cov=handler --cov-report=term-missing -v"

[tool.coverage.run]
source = ["src", "handler"]

[tool.coverage.report]
show_missing = true
